# Cross-Platform Parity Matrix

This matrix tracks BugNarrator product contracts across the native macOS app and the in-progress native Windows app.

Use [product-spec.md](product-spec.md) as the source of truth for the contracts named here. Use this matrix to document deliberate platform differences instead of letting them drift into undocumented behavior.

## Status Vocabulary

- `Shipped`: production behavior exists today
- `In Progress`: active implementation work exists, but parity is not yet proven
- `Planned`: the contract is accepted, but implementation has not started yet

## Matrix

| Contract / Spec Item | macOS | Windows | Parity Decision | Notes / Rationale |
| --- | --- | --- | --- | --- |
| Durable workflow: `record -> review -> refine -> export` | Shipped | In Progress | Must remain identical | Windows implements the core workflow, but the remaining real-desktop validation is tracked in #1133 ([WIN-014 checklist](../validation/win-014-checklist.md)). |
| Compact launch surface | Shipped as a menu bar window | In Progress as a tray shell | Native surfaces allowed | Menu bar and tray are platform-native equivalents; the remaining real-desktop tray validation is tracked in #1133. |
| Recording Controls surface | Shipped | In Progress | Must remain functionally aligned | Windows has the surface implemented; the remaining real-desktop recording-controls validation is tracked in #1133. |
| Single active recording session | Shipped | In Progress | Must remain identical | Automated coverage exists on Windows, but the remaining real-desktop runtime validation is tracked in #1133. |
| Screenshot evidence during recording | Shipped | In Progress | Native capture implementation allowed | Windows has overlay/capture plumbing, but DPI, multi-monitor, and real-desktop capture validation are tracked in #1133. |
| Session Library archive | Shipped | In Progress | Must remain identical | Implemented by Windows Milestone 5; #50 is closed as completed, with the remaining real-desktop validation tracked in #1133. |
| Review Workspace tabs | Shipped | In Progress | Must remain identical | Implemented by Windows Milestones 5 and 6; #51 is closed as completed, with the remaining real-desktop validation tracked in #1133. |
| Session Bundle export | Shipped | In Progress | `transcript.md` must remain identical; bundle contents may differ | Windows `transcript.md` was realigned to the macOS `markdownContent` contract in `WIN-011` / #995 (same section names, ordering, date/duration formats, fallback text, LF endings). Both bundles also contain a `summary.md` when issue extraction has run (macOS since #924; Windows aligned to that same emit condition in #1006). Their contents are not byte-identical yet — see the note below. The bundle *layout* is now machine-checked on both platforms against `contract-fixtures/session-bundle-layout.json` (macOS `TranscriptExporterTests.swift`, Windows `BundleExporterTests`; #1137 added the Windows side and the `manifest.json` it was missing). The remaining real-desktop validation is tracked in #1133. |
| Debug Bundle support export | Shipped | In Progress | Must remain aligned | Implemented on Windows with redaction/hardening coverage; the remaining real-desktop support-bundle validation is tracked in #1133. |
| Missing or invalid AI provider recovery | Shipped | In Progress | Must remain identical | Windows preserves completed sessions on missing, incompatible, or failed AI provider setup; the remaining real-provider validation is tracked in #1133. |
| Configurable AI provider setup | Shipped | In Progress | Must remain aligned | Windows supports OpenAI, OpenAI-compatible hosted endpoints, and local-compatible endpoints; the remaining real-provider validation is tracked in #1133. |
| Recording audio source selection | Shipped | In Progress | Platform-native capture allowed | Windows supports microphone, WASAPI loopback system audio, and mixed microphone + system audio muxed to one track (`WIN-010` / #453). The remaining real-desktop mixed-capture validation is tracked in #1133. |
| Experimental GitHub and Jira export | Shipped as experimental | In Progress | Experimental on both platforms | Windows implementation exists; the remaining real-credential validation is tracked in #1133. |
| Keyboard-first accessibility | Shipped baseline, validated in RR-005 | In Progress | Native implementation allowed | The contract is clear keyboard and assistive-tech support, not identical widgets. The remaining real-desktop keyboard and Narrator validation is tracked in #1133. |
| Public release packaging | Shipped as signed, notarized DMG | Repo-side signed tester zip path ready; certificate provisioning required | Platform-native packaging allowed | Windows first tester format is a zip containing signed `BugNarrator.Windows.exe` plus signature and package validation evidence. Installer/MSIX can follow if tester distribution requires it. |
| Launch at login | Shipped (`LaunchAtLoginService`, SMAppService) | In Progress (#1164) | Platform-native mechanism allowed | Windows registers a per-user `Run` value through an injectable registry seam; the same toggle label as macOS. Windows has no approval state. Real-desktop check: the app appears on the Windows Settings Startup page and launches at the next sign-in; tracked in #1133. |
| Bundled Sample session | Shipped (`SampleSession.swift`) | In Progress (#1165) | Must remain identical | Same id, title, creation time, transcript, markers, and three extracted issues, pinned by a test that reads the Swift file. Added to the library as a real, deletable session flagged `IsSampleSession`, offered from the empty library and from the tray only while the library is empty (`FirstRunFunnel.shouldOfferSampleSession`). Rows show a `Sample  •` prefix. Real-desktop check of both offers is on #1133. |
| Automatic issue extraction after transcription | Shipped (`autoExtractIssues`, `PostTranscriptionPipelineController`) | In Progress (#1172) | Must remain identical | Off by default on both platforms; when on, extraction follows the saved transcript and a failure keeps the transcribed session. The one-time offer that turns it on is #1166. |
| Issue extraction offer (once) | Shipped (`IssueExtractionOfferBanner`) | In Progress (#1166) | Must remain identical | Same rule (`IssueExtractionOfferPolicy` = macOS `shouldShowIssueExtractionOfferBanner`) and copy; Turn On switches automatic extraction on, both answers make the offer permanent. The provider-capability term is the profile's `SupportsIssueExtraction` (#1168). |
| Transcription-only provider rule (Local Parakeet) | Shipped (`AIProvider.parakeetLocal`, `supportsIssueExtraction`) | In Progress (#1168) | Must remain identical | `WindowsAiProviderProfile.SupportsIssueExtraction` mirrors `SettingsStore.supportsIssueExtraction`; the Local (Parakeet) profile (`parakeetLocal`, `http://localhost:8422`, no key, model `parakeet-tdt-0.6b-v3`) is pinned to the Swift source by a test. Extraction (manual, automatic, and the one-time offer) is refused before any request with the macOS guidance sentence; transcription is unaffected. Starting or bundling the server is WIN-035 (#1169). |
| First-run welcome tour | Shipped (`WelcomeView.swift`, `OnboardingFlow.swift`) | In Progress (#1167) | Must remain identical | `FirstRunFunnel` (Core) is the pure rule, mirroring `OnboardingFlow`: never shown with sessions, never after finish or skip (`HasCompletedWelcome`, durable), otherwise opens on the first incomplete step in provider → microphone → hotkeys order; step titles are pinned to the Swift enum by a test. `WelcomeWindow` uses the macOS copy, Skip Setup confirms only when the provider step still blocks recording, the close box counts as a skip, and Use Suggested Shortcuts fills empty slots only. Reopenable from the tray (Show Welcome Tour). Windows has no system microphone prompt for desktop apps, so the microphone step probes the device and links to the Privacy page instead. |
| Recovery guidance from the launch surface | Shipped (menu bar setup banner + status area, `MenuBarView+SetupBanner.swift`) | In Progress (#1160) | Must remain identical in intent | `RecordingControlState.Blocker` (`RecoveryBlocker`: Permission / Credential / Storage / Other, guidance, destination) is set wherever the lifecycle publishes a failed or refused state, including a saved session whose transcription was refused for a missing provider. The tray shows a `Fix: …` entry directly under Status that opens Settings, the recording controls, or the library; never while Idle or Recording. macOS uses a banner rather than a menu entry — a surface difference, same routing. |
| Check for Updates | Shipped (`ReleaseUpdateCheck.swift`, #961) | In Progress (#1161) | Must remain identical | `ReleaseUpdateChecker` mirrors the Swift checker: same `releases/latest` endpoint (pinned by a test that reads the Swift file), same numeric version comparison (prefix `v` and suffixes ignored, uneven component counts), same three outcomes and user messages, and the same never-dead-ends rule: up to date opens nothing, an update opens that release, a failed check opens the releases page. Tray entry after Support Development; outcome shown on the Status line. The request carries only Accept and a product User-Agent. |
| Managed local transcription server (Parakeet) | Shipped (`LocalTranscriptionManager.swift`) | In Progress (#1179 of #1178–#1181; design #1169) | Must remain identical in trust posture | `LocalTranscriptionServerManager` ports discovery and install: same GitHub Releases selection rules (non-draft, non-prerelease, both assets, size caps, project download path only, 20 pages of 30), SHA-256 + size manifest check, publisher verification before install and before every launch (Authenticode subject + chain instead of `codesign -R`), staging + atomic move, symlinked install dir refused, same state guards and messages. Install dir is `%LOCALAPPDATA%\BugNarrator\LocalTranscription`. Process launch (Job Object, shutdown grace) is #1181; health/readiness/Settings is #1180; the signed asset itself is #1178. |

## Current Deliberate Differences

- macOS is the only production platform today.
- Windows implements the core `record -> review -> refine -> export` path, including session library, review workspace, extraction, export, hotkeys, and hardening coverage, but the remaining real Windows runtime validation is tracked in #1133 ([WIN-014 checklist](../validation/win-014-checklist.md)).
- macOS currently has mixed microphone plus system audio capture beyond Windows; Windows surfaces the limitation explicitly while system-audio loopback support is available.
- Both platforms export a `summary.md`, and both now write it only when issue extraction has run. As of Tuesday, August 25, 2026, `contract-fixtures/summary.golden.md` pins the shared subset rather than the whole file. The pinned subset is: the document title, duration, transcript model, extraction summary, guidance note, and the shared per-issue fields both render today (title, category, severity, component, section, transcript time, confidence, requires-review flag, summary, evidence, dedup hint). The remaining differences are intentional and documented here instead of being hidden in per-platform literals.
- `summary.md` still has intentional structure differences outside the shared fixture. macOS groups issues under one section per category (`## Bug`, `## UX Issue`, `## Enhancement`, `## Question / Follow-up`) and emits empty `None identified.` sections for categories with no issues. Windows instead uses a single `## Extracted Issues` section with one `### <title>` block per issue. The two layouts carry overlapping data, so the fixture normalizes them to the shared subset rather than forcing either renderer to change in #1006.
- Windows `summary.md` intentionally retains fields that macOS does not render there yet: `Selected For Export`, reproduction steps, and `Note`. These are user-visible review details already present in Windows and were explicitly kept out of #1006's convergence scope.
- macOS `summary.md` can render related screenshot filenames for an issue; Windows currently does not render those lines in `summary.md`. That difference is not pinned by `summary.golden.md`.
- `summary.golden.md` intentionally does not pin `- Recorded:` yet. macOS `summaryMarkdownContent` still renders that line through machine-local `Date.formatted(...)`, while the transcript contract has an invariant timestamp path. The shared-subset reducer still asserts that the line exists; pinning its exact bytes belongs with a dedicated macOS renderer/invariant-timestamp follow-up rather than this fixture ticket.
- Windows renders screenshot-to-marker linkage from `SessionTimelineMoment.RelatedScreenshotId`, including the linked marker label in `transcript.md` (#1043).
- Decision for #996: Windows does not segment transcripts. It exports the complete raw transcript and recorded timeline markers, and deliberately omits the macOS `## Transcript Sections` block. No empty section model or inferred segmentation is added. Adding segmentation would be a separate product capability with its own data source and validation.
- `transcript.md` parity is now machine-checked rather than asserted twice: both platforms byte-compare against `contract-fixtures/transcript.golden.md` (#1003 wired up the Windows side of #1000/#1001). Doing so exposed and fixed two real Windows divergences — marker notes were not rendered at all (`SessionTimelineMoment` had no note field), and Windows appended a trailing newline macOS does not emit.
- Extracted issues now carry `severity`, `component`, and a `deduplicationHint` on Windows (`WIN-013` / #998), using the same FNV-1a hash contract as macOS so the same issue yields the same hint on both platforms. They are editable in the Windows issue editor (severity and component; the deduplication hint is shown read-only because it is a generated identity) and included in GitHub and Jira export bodies in the same order macOS uses. `reproductionSteps` are now parsed, persisted, and rendered on Windows in `summary.md` and both tracker exports, using the macOS section shapes and the same `reproductionStepLimit`/`listEntryLimit` caps and truncation markers; they are display-only in the Windows editor (macOS allows editing). `screenshotAnnotations` are now parsed, clamped with the same normalized-coordinate rules as macOS, persisted, and rendered in both tracker exports using the macOS no-rendered-asset line shape. Windows does not draw annotated PNG files — macOS does that with CoreGraphics/ImageIO, and the macOS export already has a fallback line for the un-rendered case, which is what Windows emits. The remaining slices on #998 are the annotated-image renderer and making reproduction steps editable in the Windows issue editor.
- Windows public tester distribution is blocked on external code-signing certificate provisioning, not missing repo scripts. The release entrypoint and manual GitHub Actions workflow are in place for the signed tester zip path.

## Update Rules

- Add or update a row whenever a platform-specific deviation becomes intentional.
- Do not use this document to justify undocumented drift.
- If a row changes meaningfully, update the relevant GitHub issue and the relevant implementation roadmap in the same phase. Update [docs/roadmap/roadmap.md](../roadmap/roadmap.md) only when the completed-phase or historical context changes.
