# Windows (and any non-macOS) server runtime + build tools. Mirrors requirements.txt minus
# parakeet-mlx (Apple-only); onnx-asr runs the same parakeet-tdt-0.6b-v3 weights on ONNX Runtime.
fastapi==0.136.1
uvicorn==0.47.0  # no [standard]: httptools/uvloop have no Windows arm64 wheels; h11 serves loopback fine
python-multipart==0.0.31
onnx-asr[cpu,hub]==0.12.0
numpy==2.5.3
pip==26.2.1
pyinstaller==6.20.0
